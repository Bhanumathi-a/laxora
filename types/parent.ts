export type Parent = {
    id: string
    name: string
    student: string
    phone: string
    address: string
    city: string
    pin: string
    state: string
    country: string
    schoolId: string
}