export type Student = {
    id: string

    firstName: string
    lastName: string

    gender: string

    studentId: string

    email: string
    phone: string

    address: string
    city: string
    pin: string
    state: string
    country: string

    grade: string

    image: string | null

    schoolId: string
}
